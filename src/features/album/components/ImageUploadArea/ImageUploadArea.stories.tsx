import preview from '../../../../../.storybook/preview';
import { ImageUploadArea } from '.';

const meta = preview.meta({
  component: ImageUploadArea,
});

export const Basic = meta.story({
  args: {},
});
